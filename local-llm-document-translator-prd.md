# PRD：本地 LLM 文档翻译器（便携傻瓜式启动版）

## 1. 产品概述

本产品是一个面向 Windows 的本地便携式文档翻译工具，目标用户无需安装 Python、无需配置命令行环境、无需理解模型部署细节，只需解压压缩包并双击启动，即可使用本地 LLM 对指定文档进行翻译。[cite:33][cite:35]

产品定位为“单机、轻量、可离线、低学习成本”的桌面小工具，强调傻瓜式启动、路径明确、输入简单、输出清晰，适合作为内容处理、资料阅读与跨语言工作流中的基础工具。[cite:33][cite:34]

## 2. 产品目标

### 2.1 核心目标

- 解压即用，双击即可启动。
- 用户只需指定：待翻译文档路径、当前语言、目标语言。
- 程序调用本地 LLM 执行翻译，不依赖云端 API。
- 输出结果稳定、可追踪、可重复查看。
- 保留清晰的输入、输出与日志目录，便于后续替换文件更新。[cite:33][cite:35]

### 2.2 非目标

首版不追求复杂排版保真，不以“完美还原 Word/PDF 原始样式”为目标。

首版不包含多人协作、账户系统、在线同步、自动更新、远程 API 翻译、OCR 扫描识别、术语库管理、批量工作流编排等高级功能。

首版不解决所有超长文档、多栏排版 PDF、图片型扫描件和极复杂表格的完全无损翻译问题。

## 3. 目标用户

### 3.1 主要用户画像

- 需要快速阅读外语资料的个人用户。
- 需要本地处理敏感文档、不希望上传云端的用户。
- 希望使用本地小模型完成初步翻译，再人工校对的技术型用户。
- 希望使用简单 GUI，而不是命令行工具链的普通用户。

### 3.2 典型使用场景

- 翻译日文、英文、中文等常见语言的 txt、md、docx、pdf 文档。
- 把外语说明书、设定资料、采访稿、文章草稿快速转成目标语言。
- 对隐私敏感内容做本地离线初译，再人工润色。
- 批量处理个人资料库中的小到中等长度文档。

## 4. 用户故事

- 作为用户，希望解压后双击一个 BAT 或 EXE 就能启动程序，而不是先安装一堆依赖。[cite:33]
- 作为用户，希望直接填写或选择文档路径，而不是记命令参数。
- 作为用户，希望明确选择源语言和目标语言，避免模型误判。
- 作为用户，希望翻译结果自动输出到固定目录，方便查找。
- 作为用户，希望能看到当前任务状态、进度和失败原因。
- 作为用户，希望后续更新只需替换少量文件或覆盖程序目录，而不影响日志和输出数据。[cite:33]

## 5. 功能范围

### 5.1 MVP 功能

#### 5.1.1 启动方式

- 提供 ZIP 便携包。
- 用户解压后双击 `启动翻译器.bat` 或主程序 `文档翻译器.exe` 启动。
- 程序自动打开本地 GUI 界面。
- 无需用户自行安装 Python、Git、Node 或额外运行时。[cite:33]

#### 5.1.2 输入能力

- 支持用户输入或选择本地文档路径。
- 支持拖拽文件到界面。
- 支持单文件翻译。
- MVP 支持格式：`.txt`、`.md`、`.docx`、`.pdf`。

#### 5.1.3 翻译参数

- 当前语言（source language）下拉选择。
- 目标语言（target language）下拉选择。
- 模型选择（例如 7B / 14B 对应的本地量化模型）。
- 翻译风格选项：直译 / 通顺翻译。
- 可选开关：保留段落结构、保留文件名、输出双语对照。

#### 5.1.4 处理流程

- 程序读取文档内容。
- 对文档按段或按块切分。
- 将文本送入本地 LLM 逐块翻译。
- 对结果进行合并、清洗与基础格式整理。
- 输出目标文件。

#### 5.1.5 输出能力

- 默认输出到 `Data/output/`。
- 输出文件命名规则：`原文件名.source-to-target.translated.ext`。
- 支持输出纯译文版本。
- 支持可选双语对照版本。
- 输出任务日志与错误日志。

#### 5.1.6 状态反馈

- 显示任务状态：待处理、读取中、切分中、翻译中、写出中、完成、失败。
- 显示当前处理文件名。
- 显示已完成块数 / 总块数。
- 显示失败原因与建议处理方式。

## 6. 交互设计

### 6.1 核心界面

首版界面采用单页式简洁布局：

- 左侧或上方：输入区。
- 右侧或下方：任务状态区与日志区。

### 6.2 输入区字段

- 文档路径：文本框 + 浏览按钮。
- 当前语言：下拉框。
- 目标语言：下拉框。
- 模型：下拉框。
- 输出模式：单语译文 / 双语对照。
- 启动按钮：开始翻译。
- 停止按钮：取消当前任务。

### 6.3 任务反馈区

- 当前状态。
- 当前块进度。
- 输出目录快捷打开按钮。
- 日志查看区。

### 6.4 UX 原则

- 默认值尽可能合理，减少首次使用门槛。
- 术语尽量白话，避免暴露过多底层技术概念。
- 错误提示尽量可执行，例如“模型文件不存在，请放入 models/ 目录”。
- 所有关键文件路径都能通过按钮直接打开对应目录。[cite:33]

## 7. 支持的输入与输出

### 7.1 输入格式

| 类型 | MVP 支持 | 说明 |
|---|---|---|
| TXT | 是 | 纯文本直接读取 |
| MD | 是 | 保留基本段落与标题结构 |
| DOCX | 是 | 读取文本内容，首版不保证复杂样式完全保真 |
| PDF | 是 | 以文本提取为主，扫描型 PDF 不保证效果 |

### 7.2 输出格式

| 输出类型 | MVP 支持 | 说明 |
|---|---|---|
| TXT | 是 | 最稳妥基础输出 |
| MD | 是 | 对原 md 结构做基础保留 |
| DOCX | 可选二期 | 首版可后置 |
| 双语对照 TXT/MD | 是 | 方便人工校对 |

## 8. 技术范围与假设

### 8.1 运行环境

- 目标平台：Windows 10/11。
- 默认面向本地运行。
- 本地已有可用 GPU 时优先 GPU 推理。
- 无 GPU 时允许 CPU 回退，但速度显著下降。[cite:35]

### 8.2 本地模型假设

- 使用本地量化 LLM 进行翻译。
- 模型由程序从 `models/` 目录读取或调用本地模型运行时。
- 首版优先兼容 7B 到 14B 量级模型，以兼顾效果、显存与易用性。[cite:35]

### 8.3 文档处理假设

- 文档会先提取为结构化文本或近似文本块。
- 超长文档通过切块策略处理。
- 块间衔接通过简单后处理优化，不追求复杂跨块全局一致性算法。

## 9. 目录与交付结构

建议便携目录结构如下：

```text
DocumentTranslator/
├─ 启动翻译器.bat
├─ 文档翻译器.exe
├─ App/
│  ├─ runtime/
│  ├─ models/
│  ├─ engine/
│  └─ assets/
├─ Data/
│  ├─ input/
│  ├─ output/
│  ├─ logs/
│  ├─ cache/
│  └─ config/
└─ 使用说明.txt
```

该结构支持后续小修复替换程序文件，也支持大版本覆盖 `App/` 同时保留 `Data/` 中的输出、日志和配置。[cite:33]

## 10. 核心处理流程

1. 用户启动程序。
2. 选择或输入文档路径。
3. 选择当前语言与目标语言。
4. 选择本地模型与输出模式。
5. 点击“开始翻译”。
6. 程序读取文档并提取文本。
7. 程序按块切分文本。
8. 本地 LLM 分块翻译。
9. 程序合并并清洗输出。
10. 写入输出目录并记录日志。
11. 界面提示完成，可直接打开结果文件夹。

## 11. 失败场景与容错

### 11.1 常见失败类型

- 文档路径无效。
- 文件格式不支持。
- PDF 无法提取文本。
- 本地模型不存在或加载失败。
- 显存不足导致推理失败。
- 单块文本过长导致模型报错。
- 输出目录无写入权限。

### 11.2 容错策略

- 启动前检查模型目录是否存在。
- 任务开始前检查输入文件是否存在且可读。
- 对超长块自动再切分。
- 单块失败时记录错误并可选择中断或继续。
- 所有错误写入日志并在界面显示简明提示。
- 程序异常退出后保留日志，便于排查。[cite:33]

## 12. 性能与质量要求

### 12.1 性能要求

- 首次启动时间尽量控制在可接受范围。
- 中小文档翻译流程应稳定，不因单次任务频繁闪退。
- 当本地 GPU 可用时，模型推理应优先使用 GPU。[cite:35]

### 12.2 质量要求

- 翻译结果应达到“可阅读、可校对、可继续编辑”的标准。
- 保留基础段落边界。
- 文件输出稳定且命名清晰。
- 日志足够支持问题定位。

### 12.3 UX 要求

- 首次使用不超过 3 步：启动、选文件、点开始。
- 输出路径容易找到。
- 错误提示必须人话化。
- 不要求用户学习命令行。[cite:33]

## 13. 二期功能候选

- 批量多文件队列。
- 术语表 / 自定义 prompt。
- OCR 扫描件支持。
- 更好的 PDF / DOCX 结构保留。
- 双栏对照导出为 DOCX。
- 翻译记忆缓存。
- 断点续跑。
- 自动检测语言。
- 更细的模型参数设置。

## 14. 成功标准

以下条件可视为 MVP 达标：

- 用户无需安装开发环境即可启动。[cite:33]
- 用户可以指定文档路径、当前语言、目标语言并完成一次成功翻译。
- 输出文件能被直接打开查看。
- 失败时有日志和清晰报错。
- 目录结构支持后续便携更新与数据保留。[cite:33]

## 15. 建议的后续工程文档

为便于后续直接交给 Claude Code 生成项目，建议紧接着补齐以下文档：

- `01-PRD.md`：本文件。
- `02-Architecture.md`：模块边界、运行时流程、模型适配层。
- `03-UX-Spec.md`：界面字段、状态机、提示文案。
- `04-Release-Spec.md`：ZIP 打包、目录结构、更新规则。
- `05-Task-Plan.md`：按阶段拆给 Claude Code 的开发任务单。[cite:38]
